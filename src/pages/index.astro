---
import Base from '../layouts/Base.astro'
import { CLIENT } from '../data/client.js'
import { renderHome } from '../render.js'

const html = renderHome(CLIENT)
---

<Base hero={true}>
  <Fragment set:html={html} />
</Base>
